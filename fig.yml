# Let's Chat: Fig Integration
# http://fig.sh
#
# Usage: fig up

# Let's Chat application
app:
  build: .
  links:
   - db:db
  ports:
   - 5000:5000
  volumes:
   - .:/lets-chat

# Mongo Database
db:
  image: mongo
